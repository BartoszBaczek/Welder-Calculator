namespace WelderCalculator.Model
{
    public static class Category
    {
        public enum OfElement
        {
            C, Si, Mn, P, S,
            N, Cr, Mo, Nb, Ni,
            Ti, Al, V, Cu
        }
    }
}
